CREATE TABLE curso (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    activo BIT(1) NOT NULL,
    categoria ENUM('TECNOLOGIA', 'CIENCIA', 'ARTE', 'MATEMATICA','PROGRAMACION') NOT NULL,
    nombre VARCHAR(255) NOT NULL
);